
export const BookAppointment = async (req: any, res: any) => {
    try {
        const appointPayload = req.body;
        const appointParsed = appoinProp.safeParse(appointPayload);
        const newappoint = await ()
    } catch (e) {
        
    }
}